[Definition]
_failregex = (?:authentication failure|AUTH LOGIN failed|authentication failure for|authentication failed)

# common exim auth failure patterns
failregex = (?i)authentication.*failed|AUTH.*failed

ignoreregex =
